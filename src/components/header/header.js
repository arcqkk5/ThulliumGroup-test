import "./header.scss";

const Header = () => {
  return (
    <header className="header">
      <div className="header__inner">Test task from ThalliumGroup company.</div>
    </header>
  );
};

export default Header;
